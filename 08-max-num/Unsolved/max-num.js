// Write code to return the largest number in the given array

var maxNum = function(arr) {
    return Math.max.apply(null, arr);


};


// SOLVED
// todo: another solution is sort/compare then return index 0